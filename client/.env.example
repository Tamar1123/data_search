# ── API ────────────────────────────────────────────────────────────────────────
# Optional. Defaults to empty string (relative URLs, proxied by Vite in dev).
# Set to your backend's public URL in production.
# VITE_API_URL=https://your-backend.railway.app
